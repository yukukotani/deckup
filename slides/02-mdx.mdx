---
title: MDX authoring
---

<div className="space-y-8">
  <p className="slida-kicker">Slide 2</p>

# MDX works too

Use **Markdown** for fast prose, then drop into JSX when a slide needs layout.

  <div className="mx-auto grid max-w-2xl gap-3 text-left">
    <div className="rounded-2xl border border-slate-300/50 p-4 dark:border-slate-700">
      Keyboard navigation: <kbd>←</kbd> / <kbd>→</kbd>
    </div>
    <div className="rounded-2xl border border-slate-300/50 p-4 dark:border-slate-700">
      URL state: <code>#1</code>, <code>#2</code>, ...
    </div>
  </div>
</div>
