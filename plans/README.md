# Implementation Plans

Generated by the improve skill on 2026-07-02 (deep audit at commit `c7aa912`),
then extended on 2026-07-12 with a fallback/compatibility simplification audit
at commit `d145c09`. Execute in the order below unless dependencies say
otherwise. Each executor: read the plan fully before starting, honor its STOP
conditions, and update your row when done.

## Execution order & status

| Plan | Title                                                             | Priority | Effort | Depends on     | Status | Last verified |
| ---- | ----------------------------------------------------------------- | -------- | ------ | -------------- | ------ | ------------- |
| 001  | Characterization tests for the Astro deck transformation pipeline | P1       | M      | —              | DONE   | `b6f4c0d`     |
| 002  | Harden compiled-output coupling; pin Astro-family catalog entries | P1       | M      | 001            | DONE   | `b6f4c0d`     |
| 003  | Single-pass, encoder-reusing `toSourceIndex`                      | P3       | S      | 001            | DONE   | `b6f4c0d`     |
| 004  | Consolidate duplicated Astro AST utilities and generic helpers    | P2       | M      | 001, 002, 003  | DONE   | `b6f4c0d`     |
| 005  | Cache theme layout discovery across virtual-module loads          | P2       | M      | 004            | DONE   | `b6f4c0d`     |
| 006  | Remove starter boilerplate (`apps/website`, `packages/utils`)     | P3       | S      | —              | DONE   | `b6f4c0d`     |
| 007  | Derive CLI version from package.json                              | P3       | S      | —              | DONE   | `b6f4c0d`     |
| 008  | Prove compiled Astro fallback reachability, then remove if unused | P1       | M      | —              | DONE   | `fb412ef`     |
| 009  | Remove the legacy `<layout>`-specific diagnostic                  | P2       | S      | 008 checkpoint | DONE   | `6d6098d`     |
| 010  | Fail when a theme layouts directory disappears                    | P1       | S      | 009            | DONE   | `ea3f1e0`     |
| 011  | Propagate invalid Astro code-block source spans                   | P1       | S      | 010            | DONE   | `ecb7c43`     |
| 012  | Limit `@deckup/astro` theme input to string selectors             | P1       | S      | —              | DONE   | `6950b3e`     |
| 013  | Fail fast when required Core runtime assets cannot resolve        | P1       | S      | —              | DONE   | `3d84bc5`     |
| 014  | Resolve built-in themes only through package exports              | P1       | S      | —              | DONE   | `b086cb2`     |
| 015  | Fail when CLI package version metadata is invalid                 | P2       | S      | —              | DONE   | `7360091`     |
| 016  | Remove Chromium cache error-string repair                         | P2       | S      | —              | DONE   | `aaaa5fd`     |
| 017  | Use one Pacote 22 manifest contract                               | P2       | S      | —              | DONE   | `cdb7049`     |
| 018  | Report invalid npm theme caches without replacing them            | P2       | S      | 017            | DONE   | `0fbf41a`     |
| 019  | Bound npm theme cache locking and surface recovery failures       | P3       | M      | 017, 018       | DONE   | `c6b9c0b`     |
| 020  | Remove legacy documentation redirects after traffic approval      | P3       | S      | external gate  | DONE   | `ad0990d`     |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Reconciliation log

### 2026-07-12 — Plan 008 (`fb412ef`)

- The temporary compiled-Astro sentinel fired in the direct control fixture.
  With Vite+ caching disabled, the CLI build, example Astro build/dev HTTP
  request, `@deckup/astro` tests, Web build, and both Web dev HTTP routes all
  completed without the sentinel.
- The unreachable compiled-output parser, public/test re-exports, fixtures,
  and tests were removed. This intentionally removes the externally importable
  `transformCompiledAstroDeckSource` named export for the next release.
- Reviewer verification passed: Core 114 tests, CLI 227 tests, Astro 1 test,
  example/Web builds, and `vp run ready`. One concurrent CLI build test failed
  once, then its focused rerun and the repeated full gate passed; it is recorded
  as a pre-existing intermittent test race rather than a Plan 008 regression.

### 2026-07-12 — Plan 009 (`b901ba3`, `6d6098d`)

- Removed all legacy lowercase `<layout>` recognition and migration-specific
  diagnostics while preserving recursive `PageMeta` validation.
- Reviewer-requested follow-up changed fixtures to `id="two-column"` and
  verified Deckup still selects the default `cover` layout, proving the old
  tag is ordinary framework content rather than explicit metadata.
- Reviewer verification passed: Core focused 88 tests, CLI focused 101 tests,
  and `vp run ready` (Core 114, CLI 227, Astro 1).

### 2026-07-12 — Plan 010 (`179fca5`, `ea3f1e0`)

- Removed stale theme metadata fallback when a warmed layouts directory
  disappears; the canonical discovery error now propagates.
- Reviewer-requested follow-up verifies both recovered layout ID and named-slot
  metadata replace the warm cache after delete → error → recreate.
- Reviewer verification passed: Core focused 62 tests and `vp run ready`
  (Core 115, CLI 227, Astro 1).

### 2026-07-12 — Plan 011 (`ecb7c43`)

- `getOptionalSpan` now propagates malformed numeric/UTF-8-boundary errors
  while retaining `undefined` for absent spans; its test seam remains private
  to the source module and is not re-exported from Core.
- Reviewer verification passed: Core focused 54 tests, CLI focused 84 tests,
  and `vp run ready` (Core 117, CLI 227, Astro 1). The CLI build suite showed
  intermittent, route-independent generated-layout races before passing the
  exact focused and full commands; no span-related failure occurred.

### 2026-07-13 — Plans 012–017 and 020

- **012 (`6950b3e`)**: `DeckupAstroOptions.theme` is now `string`; resolved
  object inputs fail at compile time and runtime. This is a breaking type
  change for undocumented object callers.
- **013 (`3d84bc5`)**: Core package metadata, CSS, and navigation assets are
  required at CLI integration construction; resolution failures retain their
  cause and the Core runtime directory is always added to `fs.allow`.
- **014 (`a9e77f4`, `b086cb2`)**: removed the workspace-relative built-in
  theme fallback. Reviewer revision replaced a mutable global test resolver
  with per-call injection that is not exported from Core's package index.
- **015 (`7360091`)**: invalid or missing CLI package metadata now throws;
  packed `deckup --version` still reports `0.0.4`.
- **016 (`aaaa5fd`)**: removed Chromium error-string matching, uninstall, and
  retry; retained executable post-validation and added safe manual recovery
  guidance to both CLI references.
- **017 (`cdb7049`)**: custom npm install operations now use only Pacote 22's
  `_resolved`/`_integrity` manifest contract. This is a breaking type change
  for custom operations returning legacy manifest shapes.
- **020 (`ad0990d`)**: owner explicitly approved seven old URLs becoming 404
  without telemetry. Redirects were removed; old build artifacts are absent
  and all current documentation routes remain.
- Integrated reviewer gates passed for all seven plans: all focused tests,
  package checks/builds, packed version/declaration/artifact assertions, and
  `vp run ready` (Core 120, CLI 236, Astro 3). One known intermittent CLI
  Astro build race failed once and passed its focused rerun plus the repeated
  full gate.

### 2026-07-13 — Plan 018 (`52aba72`, `0fbf41a`)

- Invalid npm theme cache entries now fail with their existing validation
  errors and are neither deleted nor replaced through network operations.
- The same-key concurrency fixture now starts genuinely uncached. Reviewer
  revision strengthened the invalid-package case to prove package metadata,
  Deckup cache metadata, and directory entries all remain unchanged.
- Reviewer/executor verification passed: CLI config 47 tests and `vp run ready`
  (Core 120, CLI 237, Astro 3).

### 2026-07-13 — Plan 019 (`d297d2a`, `deb5574`, `c6b9c0b`)

- Cache lock acquisition now has a 60-second monotonic deadline and never
  removes locks by mtime. Timeout guidance names the spec and lock path.
- Temp cleanup and lock release failures are surfaced, with ordered
  `AggregateError` values when a primary operation also fails. Rename
  collisions now fail and preserve the external entry.
- Reviewer revision removed lifecycle/clock fields from the publicly
  re-exported `DeckupNpmThemeResolveOptions`; per-call seams now exist only in
  `packages/core/src/npm-theme.ts` and are absent from built declarations and
  package indexes.
- Reviewer verification passed: CLI config 50 tests, Core/CLI checks and
  builds, declaration/privacy greps, and `vp run ready` (Core 120, CLI 240,
  Astro 3).

### 2026-07-12 — `b6f4c0d`

- **Automated gates**: `vp run deckup#test` passed (232 tests),
  `vp run @deckup/core#test` passed (124 tests), and `vp check` passed.
- **001**: the CLI characterization suite remains present with 35 tests;
  the implementation and an expanded suite also live in `packages/core/`.
- **002**: Astro-family catalog entries remain pinned to caret ranges. Astro
  has advanced deliberately from `^7.0.3` to `^7.0.4`; the former marker
  scanner has since been superseded by an Acorn AST transform, while the
  characterization suites still pass.
- **003**: `createSourceIndexConverter` still uses one module-level
  `TextEncoder`, and its ASCII, multibyte, boundary, and error tests pass.
- **004**: the original CLI helper consolidation remains intact. Shared deck
  transformation code has since moved into `@deckup/core`, which now owns
  package-local equivalents rather than importing CLI internals.
- **005**: parallel layout discovery, fingerprint caching, generated-page
  write memoization, and cache invalidation/reference-equality tests remain
  in place under `packages/core/`; the CLI compatibility tests also pass.
  The manual HMR smoke check was not repeated during this read-only
  reconciliation.
- **006**: `apps/website` and `packages/utils` remain absent, with no stale
  lockfile importer entries.
- **007**: source and existing packed CLI both report package version
  `0.0.4`; the package-version regression test passes.
- **Full gate note**: `vp run ready` was not repeated because it performs
  builds that write generated artifacts. The read-only reconciliation used
  focused tests and check-mode verification instead.

## Dependency notes

- **002, 003 require 001**: both refactor `deckup-vite-plugins.ts` internals;
  001's characterization tests pin current behavior first (001 also adds the
  `transformCompiledAstroDeckSource` test export that 002's tests use).
- **004 requires 001–003**: it moves functions inside the same files those
  plans edit; running it last avoids merge churn, and 003 explicitly notes
  how to proceed if 004 landed first (it shouldn't).
- **005 requires 004**: same two files (`deckup-vite-plugins.ts`,
  `theme-layouts.ts`); 005's import layout assumes 004's `astro-ast.ts` /
  `utils.ts` split exists.
- **006 and 007 are independent**: safe to execute at any time, including in
  parallel with the 001→005 chain (disjoint files).
- **008 must run before 009–011**: its reachability experiment needs the
  current Astro/Vite pipeline. If a real path reaches the compiled fallback,
  008 becomes BLOCKED after the probe is reverted and evidence is recorded;
  that completed checkpoint still unblocks 009. Plans 009→010→011 then run
  sequentially because all edit `packages/core/src/deckup-vite-plugins.ts`.
- **012–016 are independent**: they can run in parallel with each other and
  with the 008→011 chain, subject to normal branch isolation.
- **017→018→019 is one npm-cache chain**: 017 narrows fixture/types first,
  018 removes invalid-cache replacement and rewrites the concurrency fixture,
  then 019 changes lock/cleanup/collision policy against that final baseline.
- **020 has an external gate**: traffic evidence or explicit owner approval
  is required before changing `apps/web/astro.config.ts`; otherwise mark it
  BLOCKED without editing source.
- The focused commands in each plan use Vite+ (`vp`). The final local gate is
  `vp run ready`.

## 2026-07-12 fallback simplification scope

All vetted findings from the focused audit were selected for planning. The
set includes fail-fast cleanup of impossible/broken package states, removal
of undocumented compatibility inputs and brittle repair paths, npm cache
recovery simplification, the requested legacy `<layout>` diagnostic removal,
and a conditional compiled-output deletion experiment. Plans 008 and 020 are
deliberately conditional: uncertainty results in BLOCKED, not guessed edits.

## Findings considered and rejected (or deferred)

Audit findings NOT planned, so nobody re-audits them:

- **CI setup** (DX, highest-leverage finding of the audit): not selected by
  the maintainer in this round. `vp run ready` remains a manual gate.
- **User-facing docs** (CLI flags, deck authoring syntax, config schema,
  `vp` install prerequisite): not selected this round.
- **LICENSE + publish metadata** (themes have `publishConfig.access: public`
  but no license/description): not selected this round.
- **Error-substring match in `refreshThemeLayouts`** (`message.includes(...)`):
  the prior deferral is superseded by Plan 010, which now removes it with a
  warm-cache/directory-removal regression test.
- **Catalog `latest` strategy overall**: only the Astro-family entries are
  pinned (in Plan 002, because they amplify the compiled-output coupling);
  repinning tooling deps (tailwindcss, vite-plus, etc.) was deliberately
  left to the maintainer.
- **God-module split of `deckup-vite-plugins.ts`** (592 lines): deferred — L
  effort; Plan 004 removes the duplication half of the problem first.
- **Page markup duplication** (`runtime/components/Page.astro` vs
  `createGeneratedPageComponentSource`): deferred — needs a design decision
  on which copy is canonical.
- **`prepareRuntime` unconditional rm+cp / concurrent dev+build race**:
  rejected as minor — single-user local tool; document-only concern.
- **"Absolute path disclosure in built dist" (subagent SECURITY-01)**:
  REJECTED as false — verified `example/dist/` contains neither absolute
  paths nor the virtual deck metadata; the module exists only at
  dev/build-intermediate time.
- **"Theme package fs.allow traversal" (subagent SECURITY-02)**: rejected as
  by-design — installing an npm theme is already the code-execution trust
  boundary.
- **"@typescript/native-preview unused" (subagent DEPS-02)**: rejected as
  false — used via `dts.tsgo: true` in `apps/cli/vite.config.ts:8`.
- **Symlink check ordering in `deck.ts`, `port: 0` nullish-coalescing, MDX
  `isSelectedFile` suffix matching, comment handling in `findMatchingBrace`,
  full-AST traversal cost (PERF-05)**: rejected as not worth doing — edge
  cases with negligible real-world impact for a local single-deck tool.
- **Direction findings** (`deckup export` PDF — FRD already ready at
  `.rpiv/artifacts/discover/2026-07-02_16-27-05_deckup-export-pdf.md`;
  `deckup init`; theme layout parity; `deckup preview`): not planned here —
  the maintainer's `.rpiv` pipeline (discover→research→design→plan) owns
  feature work.
